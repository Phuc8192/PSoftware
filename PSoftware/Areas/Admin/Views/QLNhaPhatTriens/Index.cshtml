@model List<PSoftware.Models.NhaPhatTrien>

@{
    ViewBag.Title = "Danh sách nhà phát triển";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutPageAdmin.cshtml";
    var grid = new WebGrid(Model, canPage: true, defaultSort: "MaNPT", rowsPerPage: 10);
}

<style>
    .alternatingRowStyle {
        background-color: #e3f2fd;
    }

    .grid-footer {
        color: #000;
        font: 20px;
        text-align: center;
        font-weight: bold;
    }

        .grid-footer --> a {
            background-color: #ffffff;
            color: #007bff;
            padding: 8px;
        }

            .grid-footer --> a:active, a:hover {
                background-color: #ffffff;
                color: #FFAD33;
            }
</style>
<h3 style="text-align: center; padding:30px;">Danh Sách Nhà Phát Triển</h3>
<p>
    @Html.ActionLink("Thêm nhà phát triển", "Create")
</p>
<div id="gridContent">
    @grid.GetHtml(
        tableStyle: "table table-bordered table-hover",
        footerStyle: "grid-footer",
        fillEmptyRows: true,
        mode: WebGridPagerModes.Numeric,
        alternatingRowStyle: "alternatingRowStyle",
        columns: new[]  // colums in grid
        {
            grid.Column("MaNPT","Mã NPT"), //the model fields to display
            grid.Column("TenNPT","Tên NPT"  ),
            grid.Column(format: (item) => Html.ActionLink("Sửa", "Edit", "QLNhaPhatTriens",new { id = item.MaNPT },"QLNhaPhatTriensController")),
            grid.Column(format: (item) => Html.ActionLink("Xem", "Details","QLNhaPhatTriens", new { id = item.MaNPT },"QLNhaPhatTriensController")),
            grid.Column(format: (item) => Html.ActionLink("Xóa", "Delete","QLNhaPhatTriens", new { id = item.MaNPT },"QLNhaPhatTriensController")),
        }
    )
</div>
